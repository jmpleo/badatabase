#!/bin/bash

PREFIX=$(pwd)

mkdir -p ./include && \
cp *.h ./include && \
cp -R batypes ./include

cmake -B build \
      -D__DB_CONFIG_PATH="$PREFIX/config/configDB.json" \
      -D__MAINSQL_PATH="$PREFIX/sql/main.sql"
